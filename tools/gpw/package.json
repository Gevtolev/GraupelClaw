{
  "name": "gpw",
  "version": "0.1.0",
  "private": true,
  "description": "GraupelClaw team coordination CLI",
  "bin": {
    "gpw": "dist/index.js"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
